# 공개해도 되는 값만 둔다. 비밀 값은 .env.local 에 둘 것(git 에 올라가지 않음).
#
# 사이트의 실제 주소. 카카오톡·페이스북 공유 미리보기(Open Graph)는 이미지 주소가
# 전체 URL 이어야 인식하므로, index.html 이 빌드될 때 이 값을 채워 넣는다.
# 배포 주소가 바뀌면 여기 한 곳만 고치면 된다. 끝에 / 를 붙인다.
VITE_SITE_URL=https://yoala25.github.io/vaca/
